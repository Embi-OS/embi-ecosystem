# Local build configuration (sourced by common.sh if renamed to build.conf)
# Copy this file to build.conf and adjust values to match your system.

# Qt installation root
QT_ROOT="/opt/Qt"

# Qt version
QT_VERSION="6.8.5"
B2QT_KIT="apalis-imx8"

# Emscripten SDK root (for WASM builds)
EMSCRIPTEN_PATH="/opt/emsdk"

# Generator and build type
GENERATOR="Ninja"
BUILD_TYPE="Release"
REBUILD=0
OPTIMIZATION="ON"

# Project metadata
PROJECT_NAME="Voh-Ecosystem"
PROJECT_VERSION="25.11.0"
PROJECT_DESCRIPTION="Chasseral"
PROJECT_BRANCH="dev"

# Base config for Yocto build
YOCTO_DIR="$HOME/A_VOH_OS_BUILD"
YOCTO_MANIFEST_URL="git@gitlab.voh.local:projects-ecosysteme/voh-os/voh-manifest.git"
YOCTO_MANIFEST_XML="stable.xml"
YOCTO_MACHINE="$B2QT_KIT"
YOCTO_TARGET="b2qt-image-swu"
YOCTO_IMAGE_NAME="voh-image"
YOCTO_IMAGE_EXTENSION="tezi.tar"
REPO_BIN="$HOME/.local/bin/repo"
BITBAKE_EXTRA_ARGS="" # additional flags (e.g., "--runall=fetch")

RELEASE_DIR="$HOME/voh_mount/50 - PR&D/90 - PB/02-PR/LINKiX/02-Dossier électronique et pneumatique/VohEcosystem"
